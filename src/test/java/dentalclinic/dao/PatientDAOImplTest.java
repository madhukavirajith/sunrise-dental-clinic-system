package dentalclinic.dao;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Placeholder for DAO-layer tests.
 *
 * TODO (student): DAO tests hit a real database, so decide and document
 * your strategy in the report (options: a dedicated test schema on your
 * local XAMPP/WAMPP MySQL instance, an in-memory H2 database configured
 * only for the test scope, or mocking the JDBC layer). Whichever you
 * choose, justify it explicitly - this maps directly to the "concise
 * rationale for the approach adopted" criterion in Task C.
 */
class PatientDAOImplTest {

    @Test
    @Disabled("Wire up a test database strategy before enabling - see class Javadoc")
    void save_thenFindById_returnsSamePatient() {
        // TODO (student): implement once test DB strategy is decided.
    }
}
